# v3.5.11 Implementation Plan

**Spec:** `docs/superpowers/specs/2026-04-03-v3.5.11-ci-lint-truthiness-hardening-design.md`
**Branch:** `feature/v3.5.11-ci-lint-truthiness-hardening`

## Steps

### Step 1: Fix scripts lint violations and add to CI

1. Run `uv run ruff check --fix scripts/create_sample_outputs.py scripts/stress_test.py`
2. Run `uv run ruff format scripts/create_sample_outputs.py scripts/stress_test.py`
3. Manually review any remaining violations that couldn't be auto-fixed
4. Update `.github/workflows/lint.yml:30` — add both scripts to `ruff check` command
5. Update `.github/workflows/lint.yml:32` — add both scripts to `ruff format --check` command
6. Verify: `uv run ruff check scripts/` and `uv run ruff format --check scripts/`
7. Commit: `fix: add create_sample_outputs.py and stress_test.py to CI lint scope`

**Review checkpoint:** Confirm lint is clean for all scripts.

### Step 2: Advance ruff target-version to py314

1. Edit `src/cja_auto_sdr/core/config.py:169` — unquote `"SDRConfig"` → `SDRConfig`
2. Edit `src/cja_auto_sdr/generator.py:1255` — unquote `"DataViewSnapshot"` → `DataViewSnapshot`
3. Edit `src/cja_auto_sdr/generator.py:1261` — unquote `"DataViewSnapshot"` → `DataViewSnapshot`
4. Edit `pyproject.toml:78-82` — remove comment, change `target-version = "py313"` → `"py314"`
5. Verify: `uv run ruff check src/ tests/` — zero violations
6. Commit: `chore: advance ruff target-version to py314, resolve 3 forward annotations`

**Review checkpoint:** Confirm no new ruff violations introduced.

### Step 3: Fix bare truthiness in diff/git.py

1. Edit `src/cja_auto_sdr/diff/git.py:108` — `if snapshot.calculated_metrics_inventory:` → `if snapshot.calculated_metrics_inventory is not None:`
2. Edit `src/cja_auto_sdr/diff/git.py:119` — `if snapshot.segments_inventory:` → `if snapshot.segments_inventory is not None:`
3. Edit `src/cja_auto_sdr/diff/git.py:142` — `if snapshot.calculated_metrics_inventory or snapshot.segments_inventory:` → `if snapshot.calculated_metrics_inventory is not None or snapshot.segments_inventory is not None:`
4. Edit `src/cja_auto_sdr/diff/git.py:144` — `if snapshot.calculated_metrics_inventory:` → `if snapshot.calculated_metrics_inventory is not None:`
5. Edit `src/cja_auto_sdr/diff/git.py:146` — `if snapshot.segments_inventory:` → `if snapshot.segments_inventory is not None:`
6. Verify: `uv run pytest tests/ -x -q -k "git"` — all git-related tests pass
7. Commit: `fix: replace bare truthiness with explicit None checks in diff/git.py`

**Review checkpoint:** Confirm git snapshot behavior is correct for empty inventory lists.

### Step 4: Add --inventory-summary to README + CLAUDE.md path trigger

1. Edit `README.md:246` — add row after "Inventory-only output":
   `| Quick inventory stats | \`cja_auto_sdr dv_12345 --include-all-inventory --inventory-summary\` |`
2. Edit `.github/workflows/test-counts.yml:5-9` — add `'CLAUDE.md'` to paths filter
3. Commit: `docs: add --inventory-summary to README, CLAUDE.md to test-counts trigger`

**Review checkpoint:** Confirm README table renders correctly.

### Step 5: Version bump and changelog

1. Bump version in all 8 files (version.py, test_ux_features.py, test_output_content_validation.py ×3, QUICK_REFERENCE.md, QUICKSTART_GUIDE.md, CONFIGURATION.md, CLAUDE.md)
2. Add changelog entry to `CHANGELOG.md`
3. Verify: `uv run python scripts/check_version_sync.py`
4. Verify: `uv run pytest tests/ -x -q` — full suite green
5. Verify: `uv run ruff check src/ tests/ scripts/ examples/`
6. Commit: `chore: bump version to v3.5.11`

**Review checkpoint:** Final verification — all checks green, version sync confirmed.

## Completion

- Push branch, open PR against main
- All 10 required CI checks must pass (branch protection enforced since v3.5.10)
- After merge: tag `v3.5.11`, create GitHub release
